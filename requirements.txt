# Core scientific stack
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0
joblib>=1.0

# Time series utilities (reading Monash .tsf and per-series scaling)
statsmodels>=0.13

# Progress bars (mGBT boosting iteration)
tqdm>=4.60

# Experiment config (configs/config.yaml)
pyyaml>=6.0

# Excel I/O (reading data/repo_data_info.xlsx)
openpyxl>=3.0

# Quickstart example
matplotlib>=3.5

# Native extension build
cython>=0.29
pybind11>=2.11
setuptools>=60
